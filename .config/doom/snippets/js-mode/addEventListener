# -*- mode: snippet -*-
# name: addEventListener
# key: ael
# uuid: ael
# --
`(unless (eq (char-before) ?.) ".")`addEventListener('${1:DOMContentLoaded}', () => {
  `yas-selected-text`$0
})`(if (eolp) ";")`